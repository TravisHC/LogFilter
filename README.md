# LogFilter
A useful log filter when debug 

# package in linux
pyInstaller --onefile --windowed --icon=.\image\LogFilter.ico .\LogFilter.py

# package in windows
python -m PyInstaller --onefile --windowed --icon=.\image\LogFilter.ico .\LogFilter.py